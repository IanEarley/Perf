<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body, id: :blog_body %>
  </div>

  <div class="field">
    <%= form.file_field :main_img, id: :blog_main_img %>
  </div>

  <div class="field">
    <%= form.file_field :thumb_image, id: :blog_thumb_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
